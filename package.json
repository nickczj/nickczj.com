{
  "name": "nickczj-com",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "homelab:push": "bun run scripts/push-homelab-status.ts",
    "preview": "nuxt preview",
    "test": "bun test",
    "postinstall": "nuxt prepare",
    "deploy": "NITRO_PRESET=cloudflare_pages bun run build && bunx wrangler pages deploy"
  },
  "dependencies": {
    "@nuxt/content": "^3.13.0",
    "better-sqlite3": "^12.9.0",
    "katex": "^0.16.45",
    "nuxt": "^4.4.4",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  }
}
